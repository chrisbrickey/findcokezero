# Django Secret Key (REQUIRED for app to start)
# This insecure default is sufficient for local development if you are only handling test/seed data.
# In production, set a real cryptographic key.
# Generate a secret key with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-local-dev-key-change-in-production

# Google Maps API Key
# Get a free key at https://developers.google.com/maps/documentation/geocoding/get-api-key
GOOGLEMAPS_KEY=your-google-maps-api-key-here

# Database Configuration (optional)
# Uncomment and customize if needed:
# DB_USER=your-username
# DATABASE_URL=postgres://user:password@host:port/dbname
